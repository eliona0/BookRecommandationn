function addToCart(){
    const cart=document.getElementsByClassName('add-to-cart');
    const the80=document.getElementsByClassName('the80');
    if(onclick!="addToCart()"){
        return null;
    }
    else{
        push(the80);
    }
}